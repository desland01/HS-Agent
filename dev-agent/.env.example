# Dev Agent Environment Variables

# Required: Anthropic API key for Claude
ANTHROPIC_API_KEY=sk-ant-...

# Required: Linear API key for task management
# Get from: Linear Settings → API → Personal API keys
LINEAR_API_KEY=lin_api_...

# Required: GitHub personal access token for creating PRs
# Get from: GitHub Settings → Developer settings → Personal access tokens → Fine-grained tokens
# Required permissions: repo (read/write), pull_requests (read/write)
GITHUB_TOKEN=ghp_...

# Optional: Override working directory (defaults to /workspace/repo)
# WORKING_DIRECTORY=/path/to/repo

# Optional: GitHub repo URL (for Railway deployment)
# GITHUB_REPO_URL=https://github.com/username/repo.git
